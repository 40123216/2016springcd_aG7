#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../diff2html/bin/diff2html" "$@"
  ret=$?
else 
  node  "$basedir/../diff2html/bin/diff2html" "$@"
  ret=$?
fi
exit $ret
